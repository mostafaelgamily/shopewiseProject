const LoadingComponent = () => <div>Loading...</div>;

export default LoadingComponent;

/**
 * LoadingComponent:
 *
 * This component is responsible for displaying a "Loading..." message while data is being fetched.
 *
 * It does not expect any props.
 *
 * Example usage:
 * <LoadingComponent />
 */
